import 'package:flutter/foundation.dart';

import 'dart:io';

class P2P with ChangeNotifier {


  P2P() {
    // connect to booting peer -> my routing table
  }

  // ping pong + timer all checking that if the peer is alive
  // get block push mem pool and
  // send block


 setupTCPServer(){
//    ServerSocket.bind(address, port);

 }
}
